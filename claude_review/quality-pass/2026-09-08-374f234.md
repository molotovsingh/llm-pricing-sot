# llm-pricing-sot — Engineering Quality Pass · `374f234` (delta)

> **Report · 2026-09-08 09:02 IST · branch `004-clear-review-queue` · commit `374f234`** — canonical read at this commit; refine while it's `HEAD`, frozen once `HEAD` advances.
> Clean worktree — findings are against `HEAD`. Stable entry: `latest.md` · history: `../INDEX.md` · home: `claude_review/`.
> **Baseline:** [`2026-09-08-6a12f98.md`](2026-09-08-6a12f98.md) — read that report for the unit map, rubric and full evidence; this is a **delta** covering only what changed.

Delta pass over `6a12f98..374f234` (two commits, `fetch_pricing.py` +
both test modules + `autoresearch.checks.sh`). Every row below was **re-verified
against the current source**, not marked done from the commit message.

**Headline: the whole P1/P2 queue from `6a12f98` is cleared, with the fixes
verified by experiment rather than assertion.** The repo has no open P0/P1, and
the one remaining watch item is a human decision, not a code change.

## Scoreboard

| | `9ce70bc` | `6a12f98` | `374f234` |
|---|---|---|---|
| right-sized | 9 | 13 | **16** |
| brittle | 3 | 2 | **0** |
| overgrown | 1 | 1 | **0** |
| risky | 1 | 0 | 0 |
| **P1** | 2 | 1 | **0** |
| P2 | 7 | 3 | **0** |

## Cleared this delta

| Was | Sev | Verdict now | How it was verified |
|---|---|---|---|
| Suite had a scheduled failure on 2026-11-15 | P1 | ✅ cleared | `test_no_half_declared_attestation` (`tests/test_fetch_pricing.py:1310`) now asserts attestation **shape** — `negotiated is True`, non-empty note, parseable `verified_at` — and no longer asserts freshness. Expiry reaches a human through `review: attestation-expired` (`fetch_pricing.py:317-318`), the channel the pipeline already implements. **Verified by experiment:** `TestShippedOverridesFile` was run under a faked `2026-12-01` clock and passed 8/8, where the prior assertion returned `'expired'` and would have failed. A new `test_attestation_expiry_is_reported_not_fatal` pins the clock per entry and asserts the aged-out entry lands in `needs_review` — so the mechanism is now tested rather than the calendar. |
| Two superseded exit-code restatements in the source | P2 | ✅ cleared | `run` (`:480`) and `query_main` (`:773`) now **reference** `_exit_for` instead of restating the mapping. `TestSourceDoesNotRestateExitCodes` (`tests/test_contract_drift.py`) governs source prose by *absence* — a marker block cannot live in a docstring — by walking every docstring with `ast` and rejecting the `<code> <freshness-word>` shape that every restatement took. **Verified in both directions:** the guard reports `[query_main, run]` against `6a12f98` and `[]` at `HEAD`, and it carries `test_the_guard_actually_catches_a_restatement` so it cannot rot into decoration. It also caught a restatement in the fixing commit's own first draft — the replacement docstring quoted the stale phrase it was removing. |
| `query_main` — 165 lines, five handlers inline | P2 | ✅ cleared | Now a **40-line dispatcher**. Shared state moved to a `_QueryContext` built once; each action is a named function (`_query_fresh` 6, `_query_review` 21, `_query_list` 14, `_query_price` 22, `_query_cheapest` 26, `_cheapest_by_endpoint` 31, `_print_review_table` 12). Discovery loading stays at its original point rather than being made lazy, so side-effect ordering is untouched. **Verified by differential:** a 26-invocation matrix — every action, every tracked model, both TTL overrides, the missing-model and unknown-action error paths — produced byte-identical stdout, stderr and exit codes before and after (same md5), with `cache/pricing.json` unmodified. |
| Ungoverned contract copy in `autoresearch.checks.sh` | P3 | ✅ cleared | The header comment now points at `README.md`'s generated block instead of paraphrasing it (`autoresearch.checks.sh:10-12`). A repo-wide grep for the superseded phrasing outside the gate and the historical spec records returns nothing. |
| Gate's fence toggle matched only ` ``` ` | P3 | ✅ cleared | Both toggle sites use `_FENCE_RE` (` ``` ` or `~~~`), with tests for both failure directions: a marker inside a `~~~` fence is ignored, and a `~~~` example no longer swallows a later real block. |

## Still open

| Unit | Blast radius | Verdict | Action | Sev |
|---|---|---|---|---|
| `overrides.json` — `glm-5.2` attestation | The one hand-typed price in the file rests on an unconfirmed date | right-sized* | watch | P2 |

`verified_at: 2026-08-16` was derived from git history, not from anyone
confirming the z.ai rate (recorded in `README.md`'s open questions since the
first pass). It is now **safe** — expiry on 2026-11-14 will surface the entry
under `needs_review` rather than failing anything — but it is still an attested
price nobody has attested. **This is a human decision:** confirm the rate against
the z.ai plan and restamp `verified_at`. No code change can resolve it, and
fabricating a date would defeat the attestation rule entirely.

## Theme status

- **T1 — the gate stopped at the file boundary.** *Closed.* Contract prose is now
  governed in both forms: markdown by generated marker block, source by absence.
  The three ungoverned copies are gone.
- **T2 — correctness gates coupled to the wall clock.** *Closed in code, one data
  item watched.* No test now depends on when it runs. The attestation deadline
  still exists — by design — but reaches a human through the reporting channel
  instead of a red suite.

**Meta-observation.** Across three passes the risk has moved and shrunk rather
than relocated indefinitely: query surface (`9ce70bc`) → verification layer
(`6a12f98`) → one unconfirmed business fact (`374f234`). What remains is the
category no amount of engineering can close — a claim about what the operator
actually pays. That is the right residue for a source-of-truth repo to end up
with, and the attestation mechanism exists precisely to keep it visible.

The pattern worth keeping from this delta: **every fix was verified by
experiment** — a faked clock, a guard run against both the old and new source, a
byte-for-byte differential over 26 invocations — rather than by asserting the
change was made. Two of those experiments caught a real mistake in the fix
itself.

---

> **⏳ Currentness.** Valid only for commit `374f234` at the time this report was
> written, with a clean worktree. **A source edit since then can invalidate these
> findings — re-run `/quality-pass` after changes.**
